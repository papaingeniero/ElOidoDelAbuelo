v1.0-dev.32 feat: Reproductor Forense WebAudio Avanzado

El Historial de Alertas listaba los eventos mediante etiquetas nativas HTML5 `<audio>`, lo que forzaba descargas opacas en el navegador y no permitía la auditoría visual de la amplitud (picos de decibelios).

La Solución (Delegación de Carga al Cliente):
1. **Canvas Interactivo**: Para no colapsar la RAM de Xiaomi calculando ondas, se construyó un `<canvas>` en `index.html`. El dispositivo front-end descarga la pista vía `fetch()`, usa su propio chip mediante `AudioContext().decodeAudioData()` y extrae numéricamente los miles de picos PCM para pintarlos.
2. **Interfaz de Waveform (Modal)**: Los audios nativos se suprimieron a favor del botón "👁️ Analizar Pista Auditiva" iterado.
3. **Reproductividad Táctil (Seeking)**: El Canvas reacciona a los clics calculando el X en pantalla (`e.clientX`) vs la Anchura del Rectángulo y redirigiendo el audio.
4. **Amplificador de Falsos Silencios**: Una grabación silenciosa dibuja picos minúsculos. Se inyectó una magnificación logarítmica matemática `(max - min) * 5.0` con un `Math.max(1, ...)` para dar cuerpo algorítmico al Canvas.
5. **Cabezal Separado**: El puntero continuo que indicaba el paso del tracking fue suplantado por dos cursores inferior/superior evitando que el cuerpo de la onda quedase escondido debajo de un 1px blanco gigante.

### 🎓 Lecciones Aprendidas (WebGL/Canvas)
**Canvas y Variables Nativas CSS**: La declaración global estricta de `ctx.fillStyle` no traduce directamente selectores `var(--color)` extraídos del DOM. Forzar Hexadecimales directos (`#4caf50`) evadió un bug masivo de renderizado Blanco puro persistente a pesar de estar la onda calculada correctamente en memoria local.
